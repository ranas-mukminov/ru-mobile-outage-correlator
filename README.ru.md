# ru-mobile-outage-correlator 📡

[![CI](https://github.com/ranas-mukminov/ru-mobile-outage-correlator/actions/workflows/ci.yml/badge.svg)](https://github.com/ranas-mukminov/ru-mobile-outage-correlator/actions/workflows/ci.yml)
[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

**ru-mobile-outage-correlator** — это SRE-инструмент для корреляции внутренних инцидентов с публичными сигналами о сбоях крупных российских операторов связи (МТС, МегаФон, Tele2, Билайн, Ростелеком, Yota). Помогает DevOps- и SRE-командам быстро определить, вызвана ли деградация сервиса проблемами на стороне оператора, сокращая время диагностики и улучшая реакцию на инциденты.

Распространяется по лицензии **Apache-2.0**, готов к промышленному использованию и консалтинговым проектам.

## Основные возможности

- **Сбор сигналов из нескольких источников**: Статус-страницы, новостные ленты, результаты собственных проб
- **Нормализация внутренних инцидентов**: Импорт из логов, метрик (Prometheus, Loki, ELK) или пользовательского JSON
- **Интеллектуальная корреляция по времени**: Настраиваемые временные окна и пороги серьёзности
- **Географическое и операторское соответствие**: Корреляция по регионам и ASN/оператору с расчётом достоверности
- **ИИ-генерация текстов на русском**: Создание постмортемов и текстов для статус-страниц (подключаемый интерфейс LLM)
- **CLI-первый подход**: Библиотека + CLI для автоматизации и скриптов
- **Учёт приватности и соответствия**: Без секретов в коде, соблюдение robots.txt и ToS
- **Готовность к CI/CD**: Автоматизированная проверка кода, тестирование и сканирование безопасности через GitHub Actions

## Архитектура и компоненты

```
┌──────────────────────────────────────────────────────────────────┐
│                     ru-mobile-outage-correlator                   │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐         ┌──────────────────┐              │
│  │ Внешние источники│         │Внутренние источн.│              │
│  │ • Статус-страницы│         │ • Prometheus     │              │
│  │ • Новостные ленты│         │ • Loki / ELK логи│              │
│  │ • Свои пробы     │         │ • JSON-экспорты  │              │
│  └────────┬─────────┘         └────────┬─────────┘              │
│           │                            │                         │
│           └────────────┬───────────────┘                         │
│                        │                                         │
│              ┌─────────▼─────────┐                               │
│              │  Движок корреляции │                              │
│              │ • Временные окна   │                              │
│              │ • Совпадение регион│                              │
│              │ • Подсказки операт.│                              │
│              │ • Оценка достоверн.│                              │
│              └─────────┬─────────┘                               │
│                        │                                         │
│              ┌─────────▼─────────┐                               │
│              │ Генератор отчётов  │                              │
│              │ • JSON / Markdown  │                              │
│              │ • ИИ-резюме        │                              │
│              └────────────────────┘                              │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

**Основные модули:**
- `config/model.py`: Модели данных и схемы (на основе Pydantic)
- `sources_external/`: Парсеры статус-страниц, новостей и проб
- `sources_internal/`: Загрузчики метрик и логов
- `correlator/engine.py`: Алгоритм корреляции с настраиваемыми правилами
- `ai/summaries.py`: Подключаемая генерация резюме (локальные шаблоны или внешняя LLM)
- `cli.py`: Интерфейс командной строки для всех операций

## Требования

### Системные требования
- **ОС**: Linux (Ubuntu 20.04+, Debian 11+, RHEL 8+, Rocky/Alma 8+)
- **Python**: 3.10 или новее
- **ОЗУ**: минимум 512 МБ (небольшие наборы данных); рекомендуется 2 ГБ+ для продакшена
- **Диск**: 100 МБ для установки, дополнительное место для логов и отчётов

### Доступ и зависимости
- Доступ в Интернет для сбора внешних сигналов (с соблюдением robots.txt и ToS)
- Опционально: доступ к Prometheus, Loki или ELK для экспорта внутренних метрик
- Опционально: API OpenAI или совместимой LLM для ИИ-резюме (см. [документацию по интеграции ИИ](docs/ai_integration.md))

### Зависимости Python
Устанавливаются автоматически через `pip`:
- `pydantic>=2.6` (валидация данных)
- `typer>=0.12` (фреймворк CLI)
- `pyyaml>=6.0` (конфигурация)
- `rich>=13.7` (вывод в терминал)

## Быстрый старт (TL;DR)

```bash
# 1. Клонируем репозиторий
git clone https://github.com/ranas-mukminov/ru-mobile-outage-correlator.git
cd ru-mobile-outage-correlator

# 2. Устанавливаем зависимости
pip install -e .

# 3. Запускаем корреляцию с примерами данных
ru-mobile-outage-correlator correlate \
  examples/demo_small_saas/internal_incidents.json \
  examples/demo_small_saas/external_signals.json \
  --output report.json

# 4. Просматриваем отчёт
ru-mobile-outage-correlator report report.json
```

**Формат по умолчанию**: Markdown-таблица с именем сервиса, классификацией, баллами и резюме.

## Подробная установка

### Установка на Ubuntu / Debian

```bash
# Обновляем список пакетов
sudo apt update

# Устанавливаем Python 3.10+ и pip
sudo apt install python3 python3-pip python3-venv git -y

# Клонируем репозиторий
git clone https://github.com/ranas-mukminov/ru-mobile-outage-correlator.git
cd ru-mobile-outage-correlator

# Создаём виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate

# Устанавливаем пакет в режиме разработки
pip install -e .

# Проверяем установку
ru-mobile-outage-correlator --help
```

### Установка на RHEL / Rocky / Alma

```bash
# Устанавливаем Python 3.10+ (RHEL 8+)
sudo dnf install python3.11 python3.11-pip git -y

# Клонируем репозиторий
git clone https://github.com/ranas-mukminov/ru-mobile-outage-correlator.git
cd ru-mobile-outage-correlator

# Создаём виртуальное окружение
python3.11 -m venv venv
source venv/bin/activate

# Устанавливаем пакет
pip install -e .

# Проверяем
ru-mobile-outage-correlator --help
```

### Установка для разработки

```bash
# Клонируем и переходим в каталог
git clone https://github.com/ranas-mukminov/ru-mobile-outage-correlator.git
cd ru-mobile-outage-correlator

# Устанавливаем с dev-зависимостями
pip install -e .[dev]

# Запускаем тесты
pytest

# Запускаем линтер
ruff check src tests
```

## Настройка

Инструмент использует JSON-конфигурацию для параметров корреляции. Пример конфигурации:

```python
# Конфигурация корреляции (в коде или через опции CLI)
{
  "time_window_minutes": 5,          # Временной буфер до/после внешних сигналов
  "minimal_external_severity": "minor",  # Фильтр малозначимых внешних сигналов
  "minimal_error_rate_increase": 0.05   # Порог для серьёзности внутренних инцидентов
}
```

### Формат внешних сигналов

Внешние сигналы должны быть в формате JSON:

```json
[
  {
    "source_type": "news",
    "operator": "MegaFon",
    "regions": ["Самарская область", "Татарстан"],
    "severity": "major",
    "start_at": "2024-03-01T09:30:00",
    "end_at": "2024-03-01T10:15:00",
    "confidence": 0.8
  }
]
```

**Поля:**
- `source_type`: `news`, `status_page`, `probe` или `custom`
- `operator`: Название оператора (например, `MTS`, `MegaFon`, `Beeline`, `Tele2`, `Rostelecom`, `Yota`)
- `regions`: Список затронутых регионов или городов
- `severity`: `info`, `minor`, `major`, `critical`
- `start_at`, `end_at`: Временные метки в формате ISO 8601
- `confidence`: Число с плавающей точкой от 0.0 до 1.0

### Формат внутренних инцидентов

```json
[
  {
    "service_name": "payments",
    "endpoint_pattern": "/api/pay",
    "error_type": "5xx",
    "regions": ["Самарская область"],
    "operator_hint": "MegaFon",
    "metrics": {"error_rate": 0.35, "latency_p95": 2.1, "rps_change": -0.1},
    "start_at": "2024-03-01T09:34:00",
    "end_at": "2024-03-01T10:05:00"
  }
]
```

**Поля:**
- `service_name`: Идентификатор внутреннего сервиса
- `endpoint_pattern`: Затронутый API-эндпоинт или компонент
- `error_type`: Категория ошибок (например, `5xx`, `timeout`, `connection`)
- `regions`: Затронутые регионы (должны совпадать с внешними сигналами для корреляции)
- `operator_hint`: Опциональная подсказка оператора для улучшения сопоставления
- `metrics`: Произвольные метрики (частота ошибок, задержка, изменения RPS)

## Использование и типовые задачи

### Сбор внешних сигналов

Нормализация сырых внешних данных (новости, статус-страницы) в стандартный формат:

```bash
# Из новостной ленты
ru-mobile-outage-correlator collect-external \
  raw_news.json \
  --source-type news \
  --output external_signals.json

# Со статус-страницы
ru-mobile-outage-correlator collect-external \
  raw_status.json \
  --source-type status_page \
  --operator "MegaFon" \
  --output external_signals.json
```

### Сбор внутренних инцидентов

Экспорт метрик из Prometheus/Loki и преобразование в формат внутренних инцидентов:

```bash
# Пример: экспорт из JSON-дампа
ru-mobile-outage-correlator collect-internal \
  prometheus_export.json \
  --output internal_incidents.json
```

### Запуск корреляции

```bash
ru-mobile-outage-correlator correlate \
  internal_incidents.json \
  external_signals.json \
  --time-window 10 \
  --min-severity major \
  --output correlation_report.json
```

**Опции:**
- `--time-window`: Временной буфер в минутах (по умолчанию: 5)
- `--min-severity`: Минимальная внешняя серьёзность для учёта (`info`, `minor`, `major`, `critical`)
- `--output`: Путь для сохранения JSON-отчёта

### Просмотр отчёта

```bash
# Markdown-таблица (по умолчанию)
ru-mobile-outage-correlator report correlation_report.json

# JSON-вывод
ru-mobile-outage-correlator report correlation_report.json --format json
```

**Поля отчёта:**
- **Сервис**: Название внутреннего сервиса
- **Классификация**: `LIKELY_EXTERNAL_OUTAGE`, `LIKELY_INTERNAL_ISSUE`, `MIXED`, `UNKNOWN`
- **Баллы**: Достоверность корреляции (0.0-1.0)
- **Резюме**: ИИ-генерация или шаблонное описание

## Обновление

```bash
# Переходим в каталог репозитория
cd ru-mobile-outage-correlator

# Получаем последние изменения
git pull origin main

# Переустанавливаем пакет
pip install -e .

# Запускаем тесты для проверки
pytest
```

**Критические изменения**: Проверяйте [CHANGELOG.md](CHANGELOG.md) (если есть) или сообщения коммитов на предмет миграционных заметок.

## Логи, мониторинг и устранение неполадок

### Логи

CLI выводит в `stdout`/`stderr` по умолчанию. Перенаправляйте в файлы для постоянного хранения логов:

```bash
ru-mobile-outage-correlator correlate \
  internal.json external.json \
  --output report.json 2>&1 | tee correlation.log
```

### Типовые проблемы и решения

**Проблема: `ModuleNotFoundError: No module named 'ru_mobile_outage_correlator'`**

**Решение:**
```bash
pip install -e .
```

**Проблема: Пустые результаты корреляции**

**Причины и решения:**
- **Несовпадение времени**: Увеличьте `--time-window` (например, `--time-window 15`)
- **Несовпадение регионов**: Убедитесь, что `regions` во внутренних и внешних сигналах точно совпадают
- **Порог серьёзности**: Понизьте `--min-severity` до `minor` или `info`
- **Нет пересекающихся временных меток**: Проверьте правильность временных меток во входном JSON

**Проблема: `ValueError: At least one region must be provided`**

**Решение:** Убедитесь, что все внутренние и внешние сигналы содержат хотя бы один регион в списке `regions`.

**Проблема: Permission denied при запуске CLI**

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Переустановите при необходимости
pip install -e .
```

### Советы по производительности

- Для больших наборов данных (>10 000 инцидентов) рассмотрите предварительную фильтрацию по временному диапазону перед корреляцией
- Используйте `--min-severity major` для снижения шума от малозначимых внешних сигналов
- Пакетная обработка: разделите большие наборы данных на временные блоки для параллельной обработки

## Замечания по безопасности

> [!WARNING]
> Этот инструмент обрабатывает операционные данные и может содержать конфиденциальную информацию. Следуйте этим рекомендациям:

- **Изменяйте значения по умолчанию**: Паролей по умолчанию нет, но убедитесь, что API-ключи для внешних LLM хранятся безопасно (см. [интеграцию ИИ](docs/ai_integration.md))
- **Ограничьте доступ**: Запускайте инструмент в изолированных средах или за VPN при обработке продакшн-данных
- **Анонимизируйте данные**: Используйте синтетические или анонимизированные данные для тестирования и примеров (см. каталог `examples/`)
- **Соблюдайте ToS**: При сборе внешних сигналов соблюдайте robots.txt, лимиты запросов и условия использования
- **Не раскрывайте чувствительные порты**: Это CLI-инструмент без сетевого сервера; порты не открываются
- **Аудит логов**: При интеграции с внешними LLM убедитесь в наличии соглашений об обработке данных (DPA)

**Соблюдение законодательства**: При обработке данных российских операторов учитывайте 152-ФЗ (защита персональных данных), если присутствуют идентификаторы пользователей. Анонимизируйте или хэшируйте идентификаторы перед корреляцией.

## Структура проекта

```
.
├── src/ru_mobile_outage_correlator/  # Основной пакет
│   ├── ai/                            # Генерация ИИ-резюме
│   ├── config/                        # Модели данных и схемы
│   ├── correlator/                    # Движок корреляции
│   ├── sources_external/              # Парсеры внешних сигналов
│   ├── sources_internal/              # Загрузчики внутренних инцидентов
│   └── cli.py                         # Точка входа CLI
├── tests/                             # Юнит- и интеграционные тесты
├── examples/                          # Синтетические демо-данные
├── docs/                              # Расширенная документация
├── .github/workflows/                 # CI/CD-пайплайны
├── pyproject.toml                     # Метаданные проекта
├── LICENSE                            # Лицензия Apache-2.0
└── README.ru.md                       # Этот файл
```

## Как внести вклад

Мы приветствуем вклад в проект! Для участия:

1. **Откройте issue** для обсуждения новых функций, багов или улучшений
2. **Форкните репозиторий** и создайте feature-ветку
3. **Следуйте стилю кода**:
   - Используйте `ruff` для линтинга: `ruff check src tests`
   - Используйте аннотации типов (синтаксис Python 3.10+)
   - Пишите юнит-тесты для новой функциональности
4. **Отправьте pull request** с чётким описанием изменений

**Код-ревью**: Все PR требуют прохождения CI-проверок (линтинг, тесты) перед слиянием.

## Лицензия

Проект распространяется по лицензии **Apache License 2.0**. Полный текст см. в файле [LICENSE](LICENSE).

Вы можете свободно использовать, изменять и распространять это ПО в коммерческих и некоммерческих целях при условии сохранения оригинальной лицензии и уведомления об авторских правах.

## Автор и коммерческая поддержка

**Автор**: [Ranas Mukminov](https://github.com/ranas-mukminov)

Для внедрения в продакшене, аудита инфраструктуры, индивидуальных интеграций и обучения команд посетите **https://run-as-daemon.ru** или свяжитесь с автором через профиль GitHub.

**Коммерческие услуги включают:**
- Настройка и оптимизация SRE/observability-стека
- Разработка индивидуальных правил корреляции для вашей инфраструктуры
- Интеграция с Prometheus, Grafana, Loki и другими системами мониторинга
- Обучение команд управлению инцидентами и анализу первопричин
- Контракты на поддержку и сопровождение
